
# Snippets Hub — Frontend Ultra PRO+
Incluye workspaces, búsqueda avanzada, versiones, papelera, PATs y webhooks. UI pulida y 100% responsive.

## Desarrollo
```bash
cp .env.example .env
npm install
npm start
```
Asegúrate de que el backend corre en `http://localhost:5000`.
